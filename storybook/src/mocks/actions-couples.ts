// Stub for couple server actions (server-only `next/cache`).
export const renameCoupleAction = async () => ({ ok: true }) as unknown;
export const createCoupleAction = async () => ({ ok: true }) as unknown;
export const joinCoupleAction = async () => ({ ok: true }) as unknown;
