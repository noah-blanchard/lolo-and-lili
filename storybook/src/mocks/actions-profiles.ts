// Stub for the server action so `next/cache` (server-only) is never bundled.
export const updateProfileAction = async () => ({ ok: true }) as unknown;
export const updateNotificationPrefsAction = async () => ({ ok: true }) as unknown;
